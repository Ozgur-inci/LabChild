using UnityEngine;

public class DebugTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Debug.Log çalýþýyor!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
